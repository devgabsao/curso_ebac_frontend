let base = prompt("Bem-vindo a calculadora de área, digite o valor da base: ");

let altura = prompt("Digite o valor da altura: ");

function calculaArea(base, area) {

   return ("O Resultado é: " + base * altura + "m²"); }

alert (calculaArea(base, area))
